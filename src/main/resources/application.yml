spring:
  application:
    name: pitter-parker
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3307/pitterparker?allowPublicKeyRetrieval=true&useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: password
  flyway:
    enabled: true
    table: flyway_schema_history
    baseline-on-migrate: true
