spring:
  application:
    name: pitter-parker
  datasource:
    driverClassName: org.h2.Driver
    url: jdbc:h2:mem:pitterparker
    username: root
    password: password
  flyway:
    enabled: true
    table: flyway_schema_history
    baseline-on-migrate: true
